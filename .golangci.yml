version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - gosec
    - errorlint
    - misspell
    - unconvert
    - revive
    - unparam
  disable:
    - depguard
  settings:
    errcheck:
      check-type-assertions: false
      check-blank: false
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
    gocritic:
      enabled-tags:
        - diagnostic
        - style
    gosec:
      excludes:
        - G204
        - G301
        - G304
        - G306
    revive:
      rules:
        - name: package-comments
        - name: exported
          disabled: true

formatters:
  enable:
    - gofumpt

run:
  timeout: 5m
